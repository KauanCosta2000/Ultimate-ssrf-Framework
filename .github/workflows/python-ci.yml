      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright browser
        run: playwright install chromium

      - name: Syntax Check
        run: python -m py_compile ssrf_arsenal.py

      - name: Run tests
        run: python -m pytest